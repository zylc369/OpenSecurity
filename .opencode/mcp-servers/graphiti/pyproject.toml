[project]
name = "opensec-graphiti-mcp"
version = "0.1.0"
description = "Graphiti temporal knowledge graph MCP server (STUB - Phase 2 will implement)"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
