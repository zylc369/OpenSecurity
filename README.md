# OpenSecurity

AI 驱动的多领域安全分析 Agent 平台。

## 注意事项

- `.privacy-data/` 为本地配置（API Key 等），已在 `.gitignore` 中忽略，**切勿提交**
- `.opencode/` 下的 `package.json` / `bun.lock` / `node_modules/` 由 OpenCode 启动时自动生成（Plugin SDK `@opencode-ai/plugin` 安装），无需手动管理
