# 进化-通过程序进行任务初始化

.opencode/agents-rules/task-initialization.md 片段，以及引用的`.opencode/binary-analysis/knowledge-base/task-initialization.md`是概率性执行，我希望变成确定执行，它们里面的逻辑我觉得能放到插件内些代码执行。

将它们通过自然语言描述的逻辑放到 .opencode/plugins/security-analysis.ts 里面，在 checkEnvironment 函数内执行完成，如果session id找不到对应的会话数据，就说明是初次执行，那么执行初始化函数。做好逻辑抽象、日志打印。

我的想法可行吗？建议吗？