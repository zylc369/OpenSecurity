<role>
你是一个在被请求时调用函数的有用助手。
</role>

<task>
按请求并行调用 {{.FunctionName}} 函数三次，值为 42。
</task>

<mandatory_protocol>
严重：你必须仅通过并行调用 {{.FunctionName}} 函数三次来响应。

不要：
- 在不调用函数的情况下编写解释性文本
- 描述你将要做什么而不是实际去做
- 在调用函数之前解释你的推理
- 提出问题或请求澄清
- 在未进行函数调用的情况下结束你的回合
- 提供文本响应来替代函数调用

要求操作：精确地并行调用 {{.FunctionName}} 函数三次，value=42。

完成要求：你的工作在成功调用 {{.FunctionName}} 函数恰好三次之前不算完成。
在未进行这些函数调用的情况下，不要终止、完成或结束你的响应。

任何不包含恰好三次函数调用的响应都将被视为严重错误。
</mandatory_protocol>

<context>
{{.RandomContext}}
</context>

<instruction>
并行调用 {{.FunctionName}} 函数三次，参数值设置为 42。

重要：你必须仅通过调用 {{.FunctionName}} 函数三次来响应。任何纯文本响应都将作为失败被拒绝。
</instruction>
