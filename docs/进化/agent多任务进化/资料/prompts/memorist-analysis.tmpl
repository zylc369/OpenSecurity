# memorist.tmpl 分析

> 本文档仅记录对 `memorist.tmpl` 的分析结论，不复制原文档内容。
> 代码引用基于 PentAGI backend 源码（`pkg/`），格式为 `文件:行号`。

---

## 1. 原文件名

- 原文件：`memorist.tmpl`
- 渲染时由模板类型枚举 `PromptTypeMemorist`（值为字符串 `"memorist"`）定位
  - 枚举定义：`templates/templates.go:39`
  - 变量集声明：`templates/templates.go:222-238`（`AvailableVariables[PromptTypeMemorist]`）

---

## 2. 类型与用途

- **类型**：A 类 —— Agent System Prompt（子 agent 的系统提示词）。
- **定位**：长期记忆检索专家（Archivist / 档案管理员），专职于从向量数据库（long-term memory）检索历史知识与可复用方案，并在 Graphiti 启用时兼任情景记忆（episodic memory）检索。
- **职责边界**：只做"检索 + 验证 + 汇总返回"，不做渗透/编码/外部联网搜索。验证手段限定在运行时容器内（terminal/file）。
- **返回契约**：必须以 `memorist_result` 工具收尾，`result`（英语技术通道）供调用方 agent 推理，`message`（任务语言）作为执行日志收尾摘要。
- 注释佐证：`templates/templates.go:39` 处枚举注释明确为 `"retrieves knowledge from vector memory store"`。

---

## 3. 配对关系

与 `question_memorist.tmpl`（用户侧 prompt）成对使用，二者在 `handlers.go` 同一次调用中先后渲染：

- 系统 prompt：`PromptTypeMemorist` → 本文件（`handlers.go:545`）
- 用户 prompt：`PromptTypeQuestionMemorist`（`handlers.go:540`）
- 二者变量集相邻声明：`templates/templates.go:222`（system）与 `:239`（user）
- 服务器侧注册为可编辑对：`server/models/prompts.go:30`

差异：system prompt 注入工具名、运行环境、执行上下文等常量；user prompt 仅注入 `Question` / `Task` / `Subtask` / `ExecutionDetails`（`templates/templates.go:239-244`），由 `handlers.go:498-504` 组装。

---

## 4. 调用时机与完整调用链

> 说明：用户描述的入口"primary_agent 调 MemoristToolName"是典型路径之一，但**并非唯一入口**。`memorist` 工具被多个 agent 注入，凡是 `ExecutorConfig` 含 `Memorist` 字段的 agent 都可委派。下列为完整且经源码验证的链路。

### 4.1 谁会调用 `memorist`

`MemoristToolName`（字符串值 `"memorist"`，`tools/registry.go:21`）作为工具注入到以下 agent 的 executor：

| 调用方 agent | 注入点（获取 handler 并塞入 ExecutorConfig.Memorist） |
| --- | --- |
| primary_agent（flow 主控） | `providers/provider.go:719` |
| assistant | `providers/assistant.go:179` |
| pentester | `providers/performers.go:622` |
| coder | `providers/performers.go:704` |
| installer | `providers/performers.go:471` |
| enricher | `providers/performers.go:385` |
| searcher（另两处） | `providers/performers.go:110`、`:273` |

即：当任一上述 agent 判断"当前上下文信息不足，需要历史任务/方案/方法论"时，发起 `memorist` 工具调用（参见 `primary_agent.tmpl` 中 "Use memorist ONLY when information in the current context is insufficient" 的策略）。

### 4.2 完整调用链

```
某 agent 调用 memorist 工具 (MemoristToolName="memorist")
   │  args = MemoristAction{ Question, TaskID?, SubtaskID?, Message }
   ▼
fp.GetMemoristHandler(ctx, taskID, subtaskID)            providers/handlers.go:448
   │  → 返回的 ExecutorHandler 即 memorist 工具的实际处理函数
   ▼
memoristHandler(action)                                  providers/handlers.go:459
   │  1. 解析 action.TaskID / action.SubtaskID（可选硬过滤）
   │     handlers.go:462-496，失败写入 executionDetails
   │  2. 组装 memoristContext["system"]（常量注入）       handlers.go:505-521
   │  3. 组装 memoristContext["user"]                     handlers.go:499-504
   │  4. 渲染 PromptTypeQuestionMemorist (user)           handlers.go:540
   │  5. 渲染 PromptTypeMemorist (system) ← 本文件        handlers.go:545
   ▼
fp.performMemorist(ctx, taskID, subtaskID, sysTmpl, usrTmpl, question)
                                                         providers/performers.go:540
   │  1. 构造 MemoristExecutorConfig                      performers.go:552-563
   │     - SearchResult: 收尾回调（unmarshal → MemoristResult）performers.go:555-561
   │     - Summarizer:   链路摘要器                        performers.go:562
   │  2. fp.executor.GetMemoristExecutor(cfg)             tools/tools.go:1753
   │     → 装配该 agent 实际可用的工具集（见第 5 节）
   │  3. restoreChain + performAgentChain                 performers.go:569-579
   │     → LLM 在本 system prompt 约束下循环调用工具
   ▼
agent 调用 memorist_result (barrier 工具，收尾)
   │  → 触发 cfg.SearchResult 回调
   │  → 结果写入 memoristResult.Result
   ▼
performMemorist 返回 memoristResult.Result 给调用方 agent
                                                         performers.go:593
```

关键边界：`memorist_result` 在 executor 中被注册为 **barrier 工具**（`tools/tools.go:1794-1796`），调用即结束本 agent 链；这与 prompt 中"必须用 `memorist_result` 收尾"的约束在代码层一致。

---

## 5. 工具列表（`{{.XxxToolName}}` 占位符）

下表覆盖**原文档中实际出现的**工具名占位符。注入统一发生在 `handlers.go:506-519`。所有工具的常量定义集中在 `tools/registry.go:11-52`，工具 schema（FunctionDefinition）集中在 `tools/registry.go:166-432`。

> 注意：`memorist` agent 实际可用的工具集（executor 装配，`tools/tools.go:1784-1819`）与"prompt 文本中显式出现的工具名占位符"**并不完全相同**。差异见第 5.2 节。

### 5.1 原文档中出现的工具名占位符

| 占位符 | 工具实际名称 | 代码位置 | 用途 | 调用时机 |
| --- | --- | --- | --- | --- |
| `{{.MemoristResultToolName}}` | `memorist_result` | 常量 `registry.go:22`；schema `registry.go:337-341`；参数 `MemoristResult` `args.go:160-163`；executor 注册 `tools.go:1785,1790,1795` | **收尾工具（barrier）**。`result` 字段（英语）为返回给调用方 agent 的完整历史上下文详述；`message` 字段（任务语言）为执行日志的收尾摘要 | agent 完成检索/验证后**必须**调用一次以结束本链路；调用即终止 agent 循环 |
| `{{.TerminalToolName}}` | `terminal` | 常量 `registry.go:45`；schema `registry.go:167-174`；参数 `TerminalAction` `args.go:93-99`；executor 注册 `tools.go:1786,1791` | 在 Docker 容器内执行命令，用于**验证**向量库/图谱检索到的发现、提取额外细节 | 当检索结果需要运行时核验、或需从容器文件中交叉引用时调用 |
| `{{.FileToolName}}` | `file` | 常量 `registry.go:46`；schema `registry.go:175-179`；参数 `FileAction` `args.go:16`；executor 注册 `tools.go:1787,1792`（与 terminal 共用 `term.Handle`） | 读/写容器本地文件 | 需要查看 `uploads`/`resources` 等任务材料，或落地中间产物时调用 |
| `{{.GraphitiSearchToolName}}` | `graphiti_search` | 常量 `registry.go:41`；schema `registry.go:321-331`；参数 `GraphitiSearchAction` `args.go:132-146`；executor 注册 `tools.go:1816-1818`（条件：`graphitiSearch.IsAvailable()`） | 查询 Graphiti 时序知识图谱，获取**情景记忆**（agent 响应史、工具执行记录、实体关系）。支持 7 种 search_type | 仅当 `GraphitiEnabled=true` 时存在；prompt 协议要求"**先**搜 Graphiti 再搜向量库" |
| `{{.SummarizationToolName}}` | `execute_task_and_return_summary` | 常量 `cast/chain_ast.go:18`；注入 `handlers.go:511` | 链路摘要的**系统标记工具**，由 summarizer 子系统在压缩历史时自动产生，**不是 agent 可主动调用的工具** | agent 自身**绝不**调用；仅在对话历史被摘要后，以"摘要标记"形式出现在历史中，供 agent 识别（见原文"摘要感知协议"） |

### 5.2 实际装配但 prompt 未用占位符的工具（重要差异）

`GetMemoristExecutor`（`tools/tools.go:1753-1822`）实际装配的工具集为：

```
memorist_result   (barrier，必选)
terminal          (必选)
file              (必选)
search_in_memory  (条件：memory.IsAvailable())   ← tools.go:1805-1808
graphiti_search   (条件：graphitiSearch 可用)     ← tools.go:1816-1819
+ Summarizer      (隐式注入，非显式工具定义)
```

**差异点**：
- `search_in_memory`（`SearchInMemoryToolName`，值 `"search_in_memory"`，`registry.go:34`；schema `registry.go:266-275`；参数 `SearchInMemoryAction` `args.go:165-170`）**是 memorist 的核心检索工具之一**，但原文档**未使用 `{{.SearchInMemoryToolName}}` 占位符**，仅以自然语言"search-in-memory 工具的 `questions` 数组"指代。
- `AvailableVariables[PromptTypeMemorist]`（`templates/templates.go:222-238`）中也**未声明** `SearchInMemoryToolName`，因此即便想加占位符也拿不到值。
- 后果：LLM 对该工具名称/参数的认知完全依赖工具 schema 自带的 `Description`（`registry.go:268-273`），而非 prompt 文本。这与 `graphiti_search`（有专属占位符 + 整段协议说明）形成明显不对称。详见第 7 节。

---

## 6. 非工具占位符（`{{.Xxx}}`）

全部在 `handlers.go:505-521`（`memoristContext["system"]`）注入。下表按"控制类 / 信息类"分组。

### 6.1 条件控制类

| 占位符 | 注入表达式 | 注入信息 | 注入时机 / 作用 |
| --- | --- | --- | --- |
| `{{.GraphitiEnabled}}` | `fp.graphitiClient != nil && fp.graphitiClient.IsEnabled()`（`handlers.go:507`） | 布尔值，表示 Graphiti 时序知识图谱是否启用 | **每次渲染时即时计算**。控制原文档中所有 `{{if .GraphitiEnabled}}` 分支：包括"知识图谱搜索"措辞、整段 `<graphiti_search_protocol>`（原文 32-126 行）、`memory_protocol` 内的 Graphiti 优先提示。启用条件底层为 `cfg.GraphitiEnabled && cfg.GraphitiURL != ""`（`providers/providers.go:356`），且 client 初始化成功 |
| `{{.UserFiles}}` | `fp.userFilesListing()`（`handlers.go:520`） | 任务材料文件清单字符串；同时作为 `{{if .UserFiles}}` 的条件（空则隐藏整段"任务材料"小节） | 渲染时计算；非空时同时用于条件判断与内容填充 |

### 6.2 运行环境与上下文信息类

| 占位符 | 注入表达式 | 注入信息 | 注入时机 |
| --- | --- | --- | --- |
| `{{.Lang}}` | `fp.language`（`handlers.go:517`） | 本次任务/流程的工作语言（任务执行日志通道语言） | 渲染时注入；决定 `message` 日志条目与收尾 `message` 的语言 |
| `{{.Cwd}}` | `docker.WorkFolderPathInContainer`（`handlers.go:514`） | 容器内工作目录绝对路径（`uploads`/`resources` 所在基目录） | 渲染时注入 |
| `{{.DockerImage}}` | `fp.image`（`handlers.go:513`） | 运行时容器使用的 Docker 镜像名 | 渲染时注入 |
| `{{.ContainerPorts}}` | `fp.getContainerPortsDescription()`（`handlers.go:515`） | 容器端口映射的可读描述 | 渲染时注入 |
| `{{.CurrentTime}}` | `getCurrentTime()`（`handlers.go:518`） | 当前时间戳 | 渲染时注入；供 agent 构建时间线/判断时效 |
| `{{.ExecutionContext}}` | `executionContext`（`handlers.go:516`），来自 `fp.getExecutionContext(ctx, taskID, subtaskID)`（`handlers.go:454`） | 当前 Flow/Task/Subtask 的结构化执行上下文（IDs、Status、Titles、Descriptions、父子关系） | 渲染时注入；用于对齐"当前目标"与界定检索范围 |
| `{{.SummarizedContentPrefix}}` | `strings.ReplaceAll(csum.SummarizedContentPrefix, "\n", "\\n")`（`handlers.go:512`） | 链路摘要内容的前缀标记字符串（换行转义后） | 渲染时注入；用于"摘要感知协议"中识别历史里的摘要消息（前缀匹配），agent 自身**不得**使用该前缀 |
| `{{.ToolPlaceholder}}` | `providers.ToolPlaceholder` 常量（`providers/provider.go:32`），值为 `"Execute operations via function invocation - textual responses are not acceptable for task completion."` | 防绕过提示：强制结构化工具调用、禁止纯文本模拟 | 渲染时注入；固定出现在文档末尾，作为通用护栏 |

> 备注：`{{.SummarizationToolName}}`（见 5.1）虽以 `ToolName` 结尾，但本质是"系统标记工具名"占位符，仅用于摘要感知协议的识别与禁用说明，并非 agent 可调用工具。

---

## 7. 设计观察（查缺补漏）

调研中发现以下与 memorist 相关的潜在不一致/可改进点，供参考：

1. **`search_in_memory` 工具的占位符缺失**（第 5.2 节）。它是 memorist 的主力检索工具，却既无 `{{.SearchInMemoryToolName}}` 占位符，也不在 `AvailableVariables` 声明中，且未出现在 `<available_tools>` 列表区。LLM 对其认知完全依赖 schema description。对比 `graphiti_search` 享受了"占位符 + 专属协议段落"的双重强化，二者权重失衡。若希望提升向量库检索质量，可考虑补齐该占位符与显式工具说明。

2. **`<available_tools>` 区不完整**。原文"工具使用"小节仅显式列出 `file` 与 `terminal`，而实际可用的 `search_in_memory` / `graphiti_search`（以及收尾 `memorist_result`）的名称与用途未在该区集中说明，散落在语言策略、graphiti 协议段、完成要求等处。这会增加 LLM 误用概率。

3. **多入口而非仅 primary_agent**（第 4.1 节）。`memorist` 是被 6+ 个 agent 共享的委派目标；任何关于"调用时机"的文档若只写 primary_agent 入口，会遗漏 pentester/coder/installer/enricher/searcher/assistant 的委派路径。

4. **`MemoristAction.TaskID/SubtaskID` 与执行上下文的对齐**（`handlers.go:462-496`）。当调用方传入的 TaskID/SubtaskID 与当前不一致时，handler 会查询数据库并切换到被请求的 task/subtask 上下文执行检索；这一行为在 prompt 中未显式告知 agent，但其 `search_in_memory` 的 schema 已声明 "TaskID/SubtaskID will be used as strict filters"。两处语义一致，无矛盾，仅作记录。
