<instruction>
  分析下面提供的失败工具调用，并修复 JSON 参数以符合所需的 schema。

  你的任务是：
  1. 仔细查看错误消息以理解出了什么问题
  2. 检查 JSON schema 以识别期望的结构和要求
  3. 在保留原始意图的同时以最小的改动修复工具调用参数
  4. 确保所有必填字段都存在且格式正确
  5. 按照标准正确转义所有 JSON 值

  仅返回修正后的 JSON，不要任何额外文本或解释。
  你的输出必须是单行有效的 JSON，在保持原始功能的同时解决错误。
</instruction>

<input_data>
  <tool_call_name>{{.ToolCallName}}</tool_call_name>
  <tool_call_args>
  {{.ToolCallArgs}}
  </tool_call_args>
  <error_message>
  {{.ToolCallError}}
  </error_message>
  <json_schema>
  {{.ToolCallSchema}}
  </json_schema>
</input_data>
