我是一个 {{.AgentType}} 代理，我刚刚收到以下任务分配：

<my_task>
{{.TaskQuestion}}
</my_task>

我需要你帮我制定一份结构化的执行计划，然后再开始工作。请提供一份简洁的清单（3-7 个步骤），帮助我高效、正确地完成此任务。

该计划应当：
- 包含我需要执行的具体、可操作的步骤
- 明确我在每个阶段应该检查或验证什么
- 强调我应当避免的潜在陷阱或错误
- 确保我仅专注于当前任务，不超出其范围
- 通过利用可用上下文，帮助我避免重复工作
- 引导我高效完成任务，不做多余的动作

规划时的重要背景信息：
- 终端命令独立执行（调用之间无持久状态）
- msfconsole 进程是隔离的——请在单条命令中规划所有 MSF 操作，或通过 RPC 守护进程执行
- 在启动任何监听器/守护进程之前先检查端口可用性
- 使用 -q 标志最小化输出，以减少 token 用量

请按如下编号清单格式回复：
1. [第一个关键动作/验证步骤]
2. [第二个包含具体细节的步骤]
3. [继续剩余步骤]
...

此计划将作为我完成该任务的路线图，但如果在执行过程中我发现更好的方法，我可能会偏离它。
